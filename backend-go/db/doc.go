// Package db holds the plain-SQL migrations (db/migrations) and sqlc query
// sources (db/queries), plus the DB-backed acceptance tests for them. The
// migrations are the single source of truth for the schema.
package db
