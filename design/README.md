# design/ — デザインツール Penpot(ローカル)

[Penpot](https://penpot.app/) は、オープンソースのデザインツールです。ここには、Penpot を自分の PC で動かすための compose と、書き出したデザインのファイル(`.penpot`)を置きます。アプリ(`backend-go/`・`frontend/`)とは独立していて、アプリの起動・テストには影響しません。

- 開く URL: <http://localhost:9001>(ポートは `.env` で変えられます)
- ローカル専用です。**インターネットには公開しないでください**(HTTPS なし・メール確認なしの設定にしています)

## 前提

- Docker(Docker Desktop など)。空きメモリは約 1.5 GiB、ディスクは約 4 GiB(下の「動作の目安」)
- 使わないときは `down` して止めてください(数百 MiB〜1 GiB を使い続けるため)

## 初回セットアップ

```bash
# 1. 設定ファイルを作る(design/.env はコミットしない)
cp design/.env.example design/.env

# 2. Penpot の鍵を生成して design/.env に追記する(値を画面に出さない・読み返さない)
printf 'PENPOT_SECRET_KEY=%s\n' "$(openssl rand -base64 48 | tr -d '\n')" >> design/.env
```

`design/.env` は鍵を含む秘密のファイルです。`.gitignore` の対象で、エージェントも読めない設定(`.claude/settings.json` の deny)になっています。`design/.env.example` は、値の雛形(秘密なし)です。

## 起動・停止・初期化

以降のコマンドは、リポジトリのルートで実行します。`-p` はプロジェクト名です。

```bash
# 起動(初回はイメージの取得に数分かかります)
docker compose -p hamburger-penpot -f design/docker-compose.yml --env-file design/.env up -d

# 停止(データは残る)
docker compose -p hamburger-penpot -f design/docker-compose.yml --env-file design/.env down

# 初期化(データもすべて消える。ユーザー・ファイルが消えます)
docker compose -p hamburger-penpot -f design/docker-compose.yml --env-file design/.env down -v
```

## 最初のユーザーを作る

起動したら、ユーザーを 1 人作ります(対話式。メールアドレス・名前・パスワードを聞かれます)。

```bash
docker exec -ti hamburger-penpot-penpot-backend-1 python3 manage.py create-profile
```

作ったメールアドレスとパスワードで、<http://localhost:9001> にログインできます(メールの確認はありません)。
初回のログインでは、利用目的のアンケート(3 問。答えないと進めません)と、チームを作る画面が出ます。チームは「Continue without team」で飛ばせます。

## デザインの書き出しと保存(`.penpot`)

デザインの変更の履歴は、書き出した `.penpot` のファイルを、`design/files/` に置いて git で追います。

- **書き出し**: 開いているファイルの左上のメニュー(︙)→ **File** → **Download Penpot file (.penpot)**。ダウンロードしたファイルを `design/files/<画面や機能の名前>.penpot` に置きます
- **読み込み**: ダッシュボードで **Import a file**(Import a .penpot file)→ `design/files/` のファイルを選びます。初期化(`down -v`)したあとの、新しい Penpot に戻すときも、この手順です
- `.penpot` は zip のバイナリなので、**git の差分は読めません**(変更はファイル単位の更新として残ります)。変更のレビューが必要になったら、SVG の書き出しの併用などを検討します
- `design/files/sample-login.penpot` は、書き出しと読み込みの動作確認用のサンプル(ログイン画面の枠 1 つと、長方形 2 つ)です。デザインではありません

### (参考)ブラウザを使わずに書き出す・読み込む

スクリプトや自動確認では、Penpot の API を使えます(`http://localhost:9001` は自分のポートに読み替えます)。

```bash
# ログイン(cookie を保存)
curl -s -c cookies.txt -H 'Content-Type: application/json' \
  -X POST http://localhost:9001/api/rpc/command/login-with-password \
  -d '{"email":"<メール>","password":"<パスワード>"}' -o /dev/null

# 書き出し: 進捗のイベントの最後に、ダウンロード用の URL(assets/by-id/...)が返る
curl -s -b cookies.txt -H 'Content-Type: application/json' \
  -X POST http://localhost:9001/api/rpc/command/export-binfile \
  -d '{"fileId":"<ファイルの id>","version":3,"includeLibraries":false,"embedAssets":false}'
curl -s -b cookies.txt -L "<返ってきた URL>" -o design/files/<名前>.penpot

# 読み込み: project-id は、読み込み先のプロジェクト(Drafts など)の id
curl -s -b cookies.txt -X POST http://localhost:9001/api/rpc/command/import-binfile \
  -F name=<ファイル名> -F project-id=<プロジェクトの id> -F version=3 -F file=@design/files/<名前>.penpot
```

パスワードを、コマンドの引数(シェルの履歴に残る)に書く場合は、ローカルの検証用のアカウントだけにしてください。

## いまの画面のデザイン(`hamburger-evaluation.penpot`)

`design/files/hamburger-evaluation.penpot` は、**いまのアプリの画面を、そのまま Penpot に写したもの**です。これから画面を作り直す(リデザインする)ときの、「変える前の見本」と「デザインの部品・色の土台」になります。見た目は変えていません(変えるのは、この次の段階です)。

最初に出てくる言葉:

- **ボード(フレーム)**: 画面 1 枚分の枠です。Penpot の画面では「Board」と表示されます。この README では「画面の枠」と書きます
- **部品(コンポーネント)**: ボタンやカードなど、何度も使う見た目を 1 つにまとめたものです。元の部品(メインコンポーネント)を直すと、そこから作った複製に反映されます
- **スタイル**: 色や文字の大きさ・太さに、名前を付けて登録したものです。「primary(オレンジ)」のように名前で使えて、あとで色を変えると、そのスタイルを使っている所がまとめて変わります。この README の「トークン」は、これのことです

### 開いて見る

1. 「起動・停止・初期化」と「最初のユーザーを作る」で、Penpot を起動してログインする
2. ダッシュボードで **Import a file** → `design/files/hamburger-evaluation.penpot` を選ぶ(数秒で終わります)
3. 読み込んだ `hamburger-evaluation` を開く。左上の **PAGES** で、下の 4 つのページを切り替えて見る
4. 見終わったら、`down` で Penpot を止める(メモリを約 1.2〜1.3 GiB 使うため)

### ページの構成

| ページ | 中身 |
|---|---|
| `値` | 色の見本(13 色・CSS の変数名つき)と、文字のスタイルの見本、角丸(6px)。ここがトークンの一覧 |
| `部品` | 部品(コンポーネント)15 個。左の **ASSETS** タブからも使えます |
| `画面(PC)` | 幅 1280px の画面 14 枚 |
| `画面(モバイル)` | 幅 375px の画面 14 枚(同じ 14 画面) |

画面の高さは、実際の画面の高さ(内容の長さ)に合わせています。

### 画面の一覧と、Penpot の枠の名前

画面の枠の名前は、`<画面の名前> / PC` と `<画面の名前> / モバイル` です(下の表は PC。モバイルは末尾を `/ モバイル` にしたもの)。並べて比べた画像は `design/screenshots/<キー>-pc.jpg` と `<キー>-mobile.jpg` です(左が実際の画面、右が Penpot)。

| 画面の名前(枠の名前の前半) | 実際の URL | 見え方 | 比べた画像のキー |
|---|---|---|---|
| サインイン | `/signin` | 未ログイン | `signin` |
| サインイン(エラー表示) | `/signin` | 間違ったパスワードで送信した後 | `signin-error` |
| 新規登録 | `/signup` | 未ログイン | `signup` |
| メールを確認してください | `/signup` | 登録を送信した後(Check your email) | `signup-sent` |
| ショップの一覧 | `/shops` | 一般ユーザー | `shops` |
| ショップの詳細 | `/shops/:id` | 一般ユーザー | `shop-detail` |
| レビューの一覧 | `/reviews` | 一般ユーザー | `reviews` |
| レビューの詳細 | `/reviews/:id` | 一般ユーザー(自分のレビュー) | `review-detail` |
| レビューの投稿 | `/reviews/new?shop_id=:id` | 一般ユーザー | `review-new` |
| レビューの編集 | `/reviews/:id/edit` | 一般ユーザー(自分のレビュー) | `review-edit` |
| プロフィール | `/users/:id` | 一般ユーザー | `profile` |
| プロフィールの編集 | `/users/:id/edit` | 一般ユーザー | `profile-edit` |
| 管理(ショップの承認・却下) | `/admin/shops` | 管理者 | `admin-shops` |
| 管理(ショップの編集) | `/admin/shops/:id/edit` | 管理者 | `admin-shop-edit` |

画面に入っている文字(ショップ名・レビューの文など)は、開発用のサンプルデータ(`backend-go/cmd/seed`)の内容です。

### 部品の一覧

`部品` ページの 15 個です。元にした画面の見た目を、そのまま部品にしています(名前の `/` は、Penpot の左のリストでグループになります)。

| 部品 | 元にした画面 | 大きさ(px) |
|---|---|---|
| Header/PC | サインイン(PC) | 1280 × 56 |
| Header/モバイル | サインイン(モバイル) | 375 × 56 |
| Button/Primary(オレンジのボタン) | サインイン(PC) | 400 × 32 |
| Button/Secondary(灰色のボタン) | メールを確認してください(PC) | 400 × 32 |
| Button/Danger(赤のボタン) | 管理(ショップの承認・却下)(PC) | 72 × 32 |
| Input(1 行の入力欄) | サインイン(PC) | 400 × 34 |
| Textarea(複数行の入力欄) | レビューの投稿(PC) | 500 × 100 |
| Select(選ぶ欄) | レビューの投稿(PC) | 500 × 36 |
| ErrorMessage | サインイン(エラー表示)(PC) | 400 × 40 |
| Card/Review(レビューの一覧の中) | レビューの一覧(PC) | 752 × 153 |
| Card/Review(ショップの詳細の中) | ショップの詳細(PC) | 752 × 111 |
| Card/Review(プロフィールの中) | プロフィール(PC) | 752 × 124 |
| Card/Shop | ショップの一覧(PC) | 752 × 52 |
| Row/管理のショップ | 管理(ショップの承認・却下)(PC) | 752 × 122 |
| Badge(Active・Pending の印) | 管理(ショップの承認・却下)(PC) | 49 × 26 |

### 色・文字のトークンの対応表

元は `frontend/src/app/styles/globals.css` の CSS の変数です。Penpot に、次の名前で登録しています(`値` ページに見本があります)。

**色のスタイル**(Penpot の **ASSETS** → Colors、グループ名は「色」)

| CSS の変数 | Penpot の名前 | 値 | 使う所 |
|---|---|---|---|
| `--color-primary` | `primary` | `#e05c00` | ヘッダー・主なボタン・リンク |
| `--color-primary-hover` | `primary-hover` | `#c75400` | 主なボタンにマウスを乗せたとき |
| `--color-danger` | `danger` | `#dc2626` | 却下・削除のボタン |
| `--color-danger-hover` | `danger-hover` | `#b91c1c` | 赤いボタンにマウスを乗せたとき |
| `--color-secondary-bg` | `secondary-bg` | `#f3f4f6` | 灰色のボタン・絞り込みの背景 |
| `--color-secondary-hover` | `secondary-hover` | `#e5e7eb` | 灰色のボタンにマウスを乗せたとき |
| `--color-border` | `border` | `#d1d5db` | 枠線・区切り線 |
| `--color-text` | `text` | `#111827` | 本文の文字 |
| `--color-text-muted` | `text-muted` | `#6b7280` | 補足の文字(日付・作成者など) |
| `--color-error` | `error` | `#dc2626` | エラーの文字 |
| `--color-success` | `success` | `#16a34a` | Active の印 |
| (`body` の `background`) | `background` | `#fafafa` | ページの背景 |
| (変数なし) | `white` | `#ffffff` | ヘッダーの文字・入力欄やカードの背景 |

- `danger` と `error` は同じ値です。画面の図形は、同じ値なら先に登録した `danger` にひも付けています
- 変数にない色(バッジの文字 `#166534`・`#9a3412`、エラー表示の背景 `#fee2e2` と枠 `#fca5a5`、入力欄の案内の文字 `#757575` など)は、CSS の中に直接書かれている色です。スタイルにはせず、色コードのままにしています
- マウスを乗せたときの色(`*-hover`)は登録していますが、画面としては描いていません

**文字のスタイル**(Penpot の **ASSETS** → Typographies、グループ名は「文字」)

名前は `<大きさ>/<太さ>`(標準 = 400、太字 = 700)です。実際の画面で使われている組み合わせを、すべて登録しています。

| 名前 | 使う所 |
|---|---|
| `24px/太字` | 画面の見出し(h1)・プロフィールのユーザー名 |
| `24px/標準` | (星の評価の大きい表示) |
| `17.6px/太字` | ヘッダーのロゴ・レビュー一覧の小見出し |
| `17.6px/標準` | 星の評価 |
| `16px/太字` | 強調・「Danger Zone」の見出し |
| `16px/標準` | ヘッダーのリンク・レビューの本文 |
| `15.2px/太字`・`15.2px/標準` | ショップ名の表示 |
| `14px/標準` | ボタン・入力欄・ラベル |
| `13.6px/標準` | 補足の文字(Created by など) |
| `12px/標準` | 日付・バッジ・入力のヒント |

- 角丸(`--radius`): 6px(図形の角丸には、実際の画面の値をそのまま入れています)
- **余白と文字の大きさには、`globals.css` に変数がありません。** 文字の大きさは、実際の画面で使われている組み合わせを、上の表のスタイルにしました。余白は、変数がないので、スタイルとしては登録していません(各図形の位置と大きさに、実際の画面の値が入っています)。余白のルールを決めるのは、リデザインの段階です
- フォント(`--font-sans`): `system-ui, -apple-system, sans-serif`。Penpot にこの名前のフォントはないので、**Penpot に最初から入っている `sourcesanspro` で代用**しています(下の「できていないこと・注意」)

### 作り方と、その理由

**方法: Penpot の API(JSON)で作りました。** 実際の画面(frontend)をヘッドレスのブラウザ(Chromium)で開き、見えている要素の位置・大きさ・色・文字を一覧(JSON)にして、`update-file`(Penpot が図形を追加・変更する API)で、長方形・グループ・**文字(編集できるテキスト)**として登録しています。

| 方法 | 使ったか | 理由 |
|---|---|---|
| Penpot の API(`update-file`)で、DOM から取った位置・色・文字を登録する | **使った** | 位置・色・文字を正確に写せる。文字が文字のまま残る(編集できる)。何度でも同じ結果で作り直せる |
| SVG の読み込み(画面を SVG にして取り込む) | 使わなかった | Penpot の SVG の読み込みは、ブラウザの中の処理で、API から呼べない。取り込むと、文字が図形(線)になり、編集できなくなることが多い |
| ブラウザで Penpot の画面を操作して作る(キャンバスの自動操作) | 使わなかった | 座標の操作は壊れやすく、時間もかかる。API のほうが確実 |

画面が変わったら、次の「いまの画面から作り直す」で、同じ手順を繰り返せます。

### いまの画面から作り直す

アプリの画面を変えたあとに、`hamburger-evaluation.penpot` を作り直す手順です。すべて、リポジトリのルートで実行します。ブラウザは Docker の中の Chromium を使います(自分の PC に入れる必要はありません)。

```bash
# 0. 作業用のディレクトリに、スクリプトと、Playwright(ブラウザ自動操作)を用意する
WORK=/tmp/penpot-work && mkdir -p $WORK && cp design/scripts/*.js $WORK/
PW_IMAGE=mcr.microsoft.com/playwright:v1.50.1-jammy
docker run --rm -v $WORK:/work -w /work $PW_IMAGE npm i --no-save playwright@1.50.1

# 1. 実際の画面を取る。アプリ(API と frontend)を、開発用のサンプルデータ入りで起動しておく(README のルートの手順)。
#    frontend のコンテナ名は docker ps で確認する。コンテナの「localhost」を frontend にするので、Vite に断られません
docker run --rm --network container:<frontend のコンテナ名> -v $WORK:/work -w /work \
  -e BASE=http://localhost:5173 -e OUT=/work/out $PW_IMAGE node capture.js

# 2. Penpot を起動して、ユーザーを作る(「起動・停止・初期化」と「最初のユーザーを作る」)。
#    作ったパスワードを、$WORK/penpot-pw.txt に 1 行で書いておく(コマンドの履歴に残さないため。コミットしない)

# 3. Penpot に、デザインを作る(数秒。画面 28 枚・部品 15・色 13・文字 11)
python3 design/scripts/build_penpot.py --base http://localhost:9001 --email <メール> \
  --password-file $WORK/penpot-pw.txt --captures $WORK/out --map-out $WORK/map.json

# 4. 書き出す(「(参考)ブラウザを使わずに書き出す・読み込む」の export-binfile。ファイルの id は map.json の fileId)
#    → design/files/hamburger-evaluation.penpot に置く
```

- **書き出しは、作ったすぐ後に行ってください。** Penpot は、ファイルを画面(エディター・ビューアー)で開くと、縮小画像を自動で作り、書き出しに含めます(ファイルが約 1.3 MB → 約 2.1 MB になります)。中身は変わりませんが、差分が大きくなります
- 画面を足すときは、`capture.js` の `routes` と、`build_penpot.py` の `SCREEN_ORDER` に足します。部品を足すときは、`build_penpot.py` の `COMPONENTS` に足します
- 作るたびに、Penpot 上には新しいファイルができます(前のファイルは、ダッシュボードで消します)

### 見た目の確認(実際の画面と並べて比べる)

```bash
# Penpot の画面(閲覧用のビューアー)を、画像にする。map.json は、手順 3 の --map-out で作ったもの
docker run --rm --network hamburger-penpot_penpot -v $WORK:/work -w /work \
  -e PENPOT_URL=http://localhost:9001 -e PENPOT_EMAIL=<メール> -e PENPOT_PASSWORD_FILE=/work/penpot-pw.txt \
  -e MAP_JSON=/work/map.json -e OUT=/work/render $PW_IMAGE \
  sh -c 'node forward.js & sleep 1; node render_penpot.js'

# 実際の画面(左)と Penpot(右)を並べた画像にする
docker run --rm -v $WORK:/work -w /work -e MAP_JSON=/work/map.json -e OUT=/work/compare -e FORMAT=jpeg $PW_IMAGE node compare.js
```

Penpot の画面は、`PENPOT_PUBLIC_URI`(この compose では `http://localhost:9001`)と同じ URL で開かないと、「404 This page doesn't exist」になります。Docker の中から開く手順では、上のように `forward.js`(`localhost:9001` を Penpot の frontend につなぐ短い中継)を使っています。`--network` の名前は、`docker network ls` で確認できます(プロジェクト名 + `_penpot`)。

### できていないこと・注意

- **文字の見た目は、完全には同じではありません。** 実際の画面のフォントは、OS の標準のフォント(`system-ui`)ですが、Penpot に入れられるフォントは `sourcesanspro`(英数字)などです。文字の幅や字形が少し違い、Penpot では文字の位置が数 px ずれて見える所があります(ボタンの中の文字が、少し上に見える、など)。Penpot は、実際の画面で測った文字の幅に合わせて文字を描くため、実際より詰まって(または広がって)見える所もあります
- **並べた画像の「実際の画面」は、太字が細く写っています。** 画像を撮った Docker の Chromium は、太字のないフォント(WenQuanYi Zen Hei)を標準のフォントとして使うためです。デザインの値は CSS の指定(太字は 700)から取っているので、Penpot 側の太字は、CSS どおりです。また、文字の幅・折り返しの位置も、このフォントで測ったものです。自分の PC のブラウザで見ると、文字の幅が少し違う所があります(ボタンやバッジのように、文字の長さで幅が決まる所)。正確に合わせたいときは、`capture.js` を、自分の PC のブラウザ(Playwright)で実行してください
- **画面の中の図形は、部品の複製(インスタンス)ではありません。** 部品は `部品` ページにメインコンポーネントとして作ってありますが、`画面(PC)` などの中のボタンやカードは、同じ見た目の**普通の図形**です(ボタン・入力欄は、`Button/Primary` のような名前のグループにまとめてあります)。部品を直しても、画面には反映されません。リデザインで部品を使いながら画面を作り直すときに、部品から置き直してください
- **色・文字は「スタイル」として登録していて、Penpot の新しい機能「Tokens」(左の **TOKENS** タブ)には入れていません。** TOKENS タブは空です。スタイルは **ASSETS** タブにあります。Tokens への移行は、リデザインの段階で、必要かどうかを決めます
- **入力欄の右端の「▼」など、ブラウザが描く部品の見た目**(選ぶ欄の矢印など)は、写していません
- **状態は、上の一覧の 1 つずつだけです。** マウスを乗せた・入力中・読み込み中・エラー(サインイン以外)などの状態は、画面として作っていません
- **配置は、位置の固定です。** Penpot の自動配置(Auto layout)や、大きさを変えたときの追従(制約)は付けていないので、画面の幅を変えても中身は追従しません。文字を長くしても、周りは動きません
- 絵文字(ヘッダーのロゴ 🍔)は、文字のまま入れています(Penpot の環境によって、見え方が変わることがあります)
- 部品・色を後から直した場合、画面の中の図形には反映されません(上の 1 つ前と同じ理由です)

### 動作の目安(このファイル。実測)

測定: Penpot 2.17.2、Docker Desktop の VM(4 CPU・7.7 GiB)、Apple Silicon(arm64)。

| 項目 | 値 |
|---|---|
| `hamburger-evaluation.penpot` の大きさ | 約 1.3 MB(1,295,392 バイト。中身は 1,230 個のファイル) |
| 読み込み(`import-binfile`)にかかる時間 | 1 秒以内 |
| 作る時間(`build_penpot.py`) | 約 2 秒 |
| 実際の画面を取る時間(`capture.js`。28 画面) | 約 48 秒 |
| Penpot の画面を画像にする時間(28 画面) | 約 135 秒 |
| Penpot のメモリ(このファイルを開いた状態の合計) | 約 1.2〜1.3 GiB(backend 約 0.8〜0.9 GiB) |
| 並べた画像(`design/screenshots/`) | 28 枚・合計 約 1.7 MB(JPEG) |

## リデザインのラフ(`hamburger-evaluation-redesign.penpot`)

`design/files/hamburger-evaluation-redesign.penpot` は、**新しい見た目の案(ラフ)**です。いまの画面(`hamburger-evaluation.penpot`)とは別のファイルで、こちらは上書きしていません。話し合いで決めた方向(**白い背景に黒い文字**・ハンバーガーの写真が主役・マスタードの黄色は塗りだけ・**赤は削除とエラーだけ**・現代的なゴシック・日本語と英語の切り替え・PC とモバイルを同じくらい)と、「遊び」として選んだ 2 つ(**線画のバーガーを水位のように塗って見せる評価**、**空・読み込み中・見つからないときの一言**)を、最初の 3 画面と状態の画面で確かめるためのものです。色や大きさの値は「案」で、見て調整します。(最初のラフは黒い背景で、次に、5 段の簡単なバーガーの白い背景版でした。評価のバーガーは、比べた 2 案のうち、水位の案(案 B)に決まったので、すべての画面を案 B に作り直しています。)

### 開いて見る

「いまの画面のデザイン」の「開いて見る」と同じ手順です(**Import a file** → `design/files/hamburger-evaluation-redesign.penpot`)。**PAGES** は、次の 18 個です(最初の 4 つが最初のラフ、次の 6 つが「リデザインの画面(第 1 弾)」、残りの 8 つが「リデザインの画面(第 2 弾)」です)。

| ページ | 中身 |
|---|---|
| `値` | 色 15 色、文字と背景のコントラストの実測、評価のバーガー(0〜5)、文字のスタイル、キーボード操作の表示の見本 |
| `画面(PC)` | 幅 1280px の画面 6 枚 |
| `画面(モバイル)` | 幅 375px の画面 6 枚 |
| `評価アイコンの比較` | 評価のバーガーの案 B(採用)と案 A(参考)を、大きさ 4 つ・評価 7 つ・色と白黒で並べた表と、色の実測 |
| `認証(PC)`・`認証(モバイル)` | サインイン・新規登録・確認メール・メールの確認(第 1 弾) |
| `レビューの投稿・編集(PC)`・`(モバイル)` | 投稿・編集・写真の欄の状態・削除の確認(第 1 弾) |
| `ショップ(PC)`・`(モバイル)` | ショップの詳細(3 つの状態)・ショップの追加(第 1 弾) |
| `プロフィール(PC)`・`(モバイル)` | 自分・他のユーザーのプロフィール・編集・アカウント削除の確認・状態の見本(第 2 弾) |
| `管理(PC)`・`(モバイル)` | ショップの管理(承認・却下)・却下の理由・ショップの編集(第 2 弾) |
| `アプリの接続(PC)`・`(モバイル)` | 許可の画面(状態つき)・サインイン(アプリの接続の途中)(第 2 弾) |
| `見え方(PC)`・`(モバイル)` | 見え方の一覧と、権限で見え方が変わる画面の変種(第 2 弾) |

画面の並べた画像は `design/screenshots/redesign/` にあります(Penpot の閲覧画面を画像にしたもの。`values.jpg` は `値` ページ、`rating-icons.jpg` は `評価アイコンの比較` ページ)。

| 画面 | 枠の名前(PC / モバイル) | 画像 |
|---|---|---|
| ショップの一覧(日本語) | `ショップ一覧 / PC`、`ショップ一覧 / モバイル` | `shops-pc.jpg`、`shops-mobile.jpg` |
| レビューの一覧 | `レビュー一覧 / PC`、`レビュー一覧 / モバイル` | `reviews-pc.jpg`、`reviews-mobile.jpg` |
| レビューの詳細 | `レビュー詳細 / PC`、`レビュー詳細 / モバイル` | `review-detail-pc.jpg`、`review-detail-mobile.jpg` |
| ショップの一覧(英語) | `ショップ一覧(English) / PC`、`ショップ一覧(English) / モバイル` | `shops-en-pc.jpg`、`shops-en-mobile.jpg` |
| 空・読み込み中・エラー・見つからない(日本語) | `空・読み込み・エラー・404 / PC`、`空・読み込み・エラー・404 / モバイル` | `states-pc.jpg`、`states-mobile.jpg` |
| 空・読み込み中・エラー・見つからない(英語) | `空・読み込み・エラー・404(English) / PC`、`空・読み込み・エラー・404(English) / モバイル` | `states-en-pc.jpg`、`states-en-mobile.jpg` |
| 評価のバーガーの比較 | `評価アイコンの比較`(`評価アイコンの比較` ページ) | `rating-icons.jpg` |

### 案の中身

- **ショップの一覧**: 写真をポスターのように大きく見せ、店名と評価(バーガー・数字・件数)は、写真の下に黒い文字で置きます。PC は 3 列、モバイルは 1 列です。審査待ちの店には、写真の上に「審査待ち」の札(白い面・黒い枠と文字)、レビューがまだない店には、塗りのないバーガーと「まだレビューがありません」を出します
- **レビューの一覧**: 1 件ずつのカードで、上に写真、下にバーガーと数字・日付・コメント・バーガーとお店の名前・投稿した人を並べます。PC は 2 列、モバイルは 1 列です。右下の「詳しく見る →」(黒い文字に下線)で詳細に進みます。写真のない投稿は、写真の場所に薄い灰色の面と「写真なし」を出しています(見せ方は、あとで決めます)
- **レビューの詳細**: 上から順に、大きな写真 → バーガー名とお店の名前 → 平均評価(大きな数字と、大きなバーガーと件数。「レビューを書く」ボタンつき)→ レビューを新しい順に縦に並べます。PC もモバイルも同じ流れです。開いているレビューは、黒い太い枠と薄い灰色の面、黄色の札「表示中のレビュー」で分かるようにし、自分の投稿には「編集」「削除」を出します。**「削除」は、赤い枠と赤い文字の白いボタン**です(赤の使い方は、下の「色と文字の値」)
- **評価のバーガー(遊びの 1 つ目。案 B「水位」)**: 黒い輪郭の 1 つのバーガーの線画(ごま付きのバンズ・ちぢれたレタス・トマトの輪切り・とろけるチーズ・焼き目のあるパティ・下のバンズ)を、**下から、評価 ÷ 最大値の高さまで、水位のように塗ります**。塗りは、バンズの茶・パティの焦げ茶・チーズのマスタード黄・レタスの緑・トマトの赤の、控えめな本物の色で、水位より上は、灰色の輪郭と白い面(白抜き)です。水位は**連続値**(4.7 は 94%)で、**数字と同じ 1 桁に丸めた値**で塗るので、数字と水位はずれません。**数字は必ずそばに書きます**(形や色だけで伝えません)。大きさは、大(幅 160px。詳細の平均・空と読み込み中の画面)・中(64px。ショップ一覧・レビュー一覧のカード)・小(40px。詳細の 1 件ごと)・極小(24px。いまの画面では使っていません)の 4 つで、小と極小は、ちぢれたレタスやトマトの輪切りなどの細部をやめて部品を太くした**簡略版の線画**です(細部は、大と中だけ)。形は、Penpot のパス(曲線)で作っています(絵は使っていません)。`値` ページに、0・1・2・3・4・4.7・5 の見本があります。
  - 案 A(部品ごと。下から 1 点 = 1 部品を塗る。半分は、その部品の左半分)は、**比べるための参考**として、`評価アイコンの比較` ページに残しています(却下ではありません)。案 A は、どの大きさでも同じ線画です(小さいとき、部品の縁が細かすぎて、ごちゃつきます)
- **一言の文言(遊びの 2 つ目)**: 空・読み込み中・見つからないとき(404)だけです。日本語は「まだ誰も食べていません」「焼いています…」「売り切れです」(下に「ページが見つかりません」)、英語は "Nobody's eaten here yet"・"Grilling…"・"Sold out"(下に "Page not found")です。**空の画面は、塗りのないバーガー**と「レビューを書く」ボタン、**読み込み中の画面は、水位が途中(60%)のバーガー**と灰色の棒(水位が上がるイメージ。ラフでは静止画)、見つからない画面は 404 の大きな数字と「ショップ一覧へ戻る」ボタンです。404 は、エラーとしては扱わず、赤にしていません
- **エラーの表示(見本 1 つ)**: 保存に失敗したときの表示です。**赤い枠 + 左の赤い帯 + 「!」のアイコン + 文言**で、見出し(「レビューを保存できませんでした」。この見出しは画面側の言葉)の下に、**API が返した文言を、そのまま**(例: `Comment is too long (maximum is 2000 characters)`。実際の backend の文言)出します。**API が返す検証・権限・通信のエラーの文言は、置き換えません**(それは backend が決めます)。状態の画面(`空・読み込み・エラー・404`)の一番下に置いています
- **英語**: ヘッダーの `JA` / `EN` で切り替える想定です。英語のショップの一覧と、状態の画面を、PC とモバイルで 1 枚ずつ作り、文字が長くなっても収まることを確かめています(店名は投稿された文字のままで、翻訳しません)
- **ロゴ**: 文字だけです。`Hamburger`(太字・黒)と、黄色の面の上に置いた `Evaluation`(標準・黒)を、同じ Noto Sans JP で組んでいます。**黄色は面だけで、文字の色にしません**。イラストは使いません
- **押せるものの見分け**: ボタンは黄色の面に黒い文字(主な操作)か、白い面に灰色の枠と黒い文字(ほかの操作)、**赤い枠と赤い文字(削除)**、リンクは黒い文字に下線、選んでいるタブは黒い下線、選んでいる言語は黒い面に白い文字です

### 評価のバーガー: 小さいときの見分けやすさ(案 B)

水位の違いは、アイコンの高さのうち、**塗られていない上の部分の高さ**で見分けます(バーガーが占める高さは、アイコンの高さの 96%)。

| 大きさ | 4 と 5 の差 | 4.5 と 5 の差 | 見え方(画像で確かめた) |
|---|---|---|---|
| 大(幅 160px) | 25.6px | 12.8px | はっきり見分けられる |
| 中(幅 64px) | 10.2px | 5.1px | はっきり見分けられる |
| 小(幅 40px。簡略版) | 6.4px | 3.2px | 4 と 5 は見分けられる。4.5 と 5 は目を凝らす |
| 極小(幅 24px。3 部品の簡略版) | 3.8px | 1.9px | 4 と 5 は、拡大すると分かる(実寸では上のバンズの色の差だけ)。**4.5 と 5 は、ほぼ見分けられない**(数字が伝える) |

- **白黒(灰色に変換)にしても**、4 つの大きさのすべてで、塗った所と白抜きの所は見分けられました(`評価アイコンの比較` ページの「白黒」の行。目で確かめたもので、数値の測定ではありません)。塗りの色の白黒での明るさは、バンズ `#aaaaaa`・パティ `#4b4b4b`・チーズ `#b8b8b8`・レタス `#a2a2a2`・トマト `#6c6c6c` で、白との比は 1.98〜8.72:1 と低いものが多いので、**塗ったかどうかは、輪郭が黒(白との比 18.88:1)か灰色(3.29:1)かにも助けられています**
- 弱点は、小さい 2 つの大きさの「4.5 と 5」です。そのため、**極小は、数字が必ず隣にあるところだけで使い**、いまの画面では、小(40px)以上にしています
- 小さい大きさの簡略版は、水位の高さが読めるように、部品を太くしています(40px は 5 部品、24px は 3 部品)。塗りの色は、大きさによらず同じです

### 色と文字の値(案)

**色のスタイル**(Penpot の **ASSETS** → Colors、グループ名は「色」。`値` ページに見本があります)

| 名前 | 値 | 使う所 |
|---|---|---|
| `background` | `#ffffff` | ページ・カード・入力欄の背景 |
| `surface-2` | `#f3f2ee` | 選択中のカード・写真なしの場所・読み込み中の灰色の棒 |
| `border` | `#dedcd6` | 区切り線・カードの枠(飾り) |
| `control-border` | `#6b6b66` | 入力欄・ボタンの枠(部品の境界) |
| `text` | `#111111` | 本文・見出し・リンク・選択中の下線と枠・バーガーの輪郭 |
| `text-muted` | `#5a5a55` | 補足の文字(日付・件数) |
| `accent` | `#e9b824` | 差し色(マスタード黄)。**塗りだけ**: ボタンの面・札の面・ロゴの面・バーガーのチーズ |
| `on-accent` | `#111111` | 黄色い面の上の文字(黒) |
| `burger-bun` | `#d8a45b` | 評価のバーガー: バンズの塗り |
| `burger-patty` | `#6b4432` | 評価のバーガー: パティの塗り |
| `burger-lettuce` | `#86b25a` | 評価のバーガー: レタスの塗り |
| `burger-tomato` | `#d1533d` | 評価のバーガー: トマトの塗り |
| `burger-empty` | `#8e8e89` | 評価のバーガー: 水位より上の輪郭(灰色) |
| `danger` | `#b3261e` | 赤。**削除とエラーだけ**: 削除ボタンの枠と文字・エラーの枠と帯と「!」の面と見出し |
| `danger-surface` | `#fdf0ef` | エラーの表示の薄い赤の背景(文字は黒) |

- **黄色は、白い地では文字にも線にも使いません**(白との比が 1.85:1 で、文字の基準 4.5:1 にも、図形の基準 3:1 にも届かないため)。黄色は「塗り」だけに使い、その上の文字は黒にします(10.20:1)
- ボタンの黄色い面と白い背景の境目の比も 1.85:1 で、境目の色には頼っていません。ボタンは、黄色の面の上の**黒い文字**で見分けます
- **赤は、削除とエラーだけ**に使います(利用者の決定。マスタード黄とバーガーの色は、そのまま)。**色だけに頼りません**: 削除ボタンは「赤い枠 + 赤い文字 + 「削除」の言葉」、エラーの表示は「赤い枠 + 左の赤い帯 + 「!」のアイコン + 文言」です。白い地での比は、赤い文字 6.54:1(基準 4.5:1)、赤い枠と帯 6.54:1(基準 3:1)です
- バーガーの塗りの色は、**このアイコンの中だけ**、控えめな本物の色を使います(画面のほかの部分は、白・黒・マスタード黄・赤のまま)。値のもとは `design/redesign/rating-icons.js` です
- ダーク(暗い背景)は作りません

**文字**: Noto Sans JP(Google Fonts)を、標準(400)・中(500)・太字(700)で使います。大きさは、`値` ページの見本のとおりです(見出し 32px・モバイルの見出し 28px・カードの店名 22px・小見出し 20px・本文 16px・ボタンと補足 14px・札 12px)。**Penpot の画面で文字を描くには、Google Fonts の読み込みが必要**で、インターネットにつながっていないと、代わりのフォントで表示されます

### 文字と背景のコントラスト(WCAG AA。実測)

`値` ページの表と同じです。ページの中で、実際の色の値から計算しています(`design/redesign/tokens.html`)。文字は 4.5:1、部品の境目や図形は 3:1 が基準です。

| 組み合わせ(前景 / 背景) | 比率 | 基準 | 結果 |
|---|---|---|---|
| 本文・リンクの文字(黒) / ページの背景 | 18.88:1 | 4.5:1 以上 | ○ |
| 本文の文字(黒) / 選択中のカードの背景 | 16.86:1 | 4.5:1 以上 | ○ |
| 補足の文字 / ページの背景 | 6.93:1 | 4.5:1 以上 | ○ |
| 補足の文字 / 選択中のカード・写真なしの場所の背景 | 6.19:1 | 4.5:1 以上 | ○ |
| ボタン・札・ロゴの文字(黒) / 黄色の面 | 10.20:1 | 4.5:1 以上 | ○ |
| 言語切替の選んだ側の文字(白) / 黒の面 | 18.88:1 | 4.5:1 以上 | ○ |
| 写真の仮の場所の文字(黒) / 暗い側の色 `#a5714a` | 4.55:1 | 4.5:1 以上 | ○ |
| 削除ボタンの赤い文字 / 白い面 | 6.54:1 | 4.5:1 以上 | ○ |
| エラーの見出しの赤い文字 / 薄い赤の背景 | 5.88:1 | 4.5:1 以上 | ○ |
| エラーの文言の黒い文字 / 薄い赤の背景 | 16.98:1 | 4.5:1 以上 | ○ |
| エラーの「!」の白い文字 / 赤の面 | 6.54:1 | 4.5:1 以上 | ○ |
| 入力欄・ボタンの枠 / ページの背景 | 5.36:1 | 3:1 以上 | ○ |
| バーガーの輪郭・キーボードで選んだ枠・選択中の枠(黒) / ページの背景 | 18.88:1 | 3:1 以上 | ○ |
| 選択中のカードの枠(黒) / カードの背景 | 16.86:1 | 3:1 以上 | ○ |
| バーガーの水位より上の輪郭(灰色) / ページの背景 | 3.29:1 | 3:1 以上 | ○ |
| 削除ボタンの赤い枠・エラーの赤い枠と帯 / ページの背景 | 6.54:1 | 3:1 以上 | ○ |
| エラーの赤い枠・帯 / 薄い赤の背景 | 5.88:1 | 3:1 以上 | ○ |
| (使わない)黄色の文字 / ページの背景 | 1.85:1 | 4.5:1 以上 | 使わない(届かない) |
| (使わない)黄色の線・枠 / ページの背景 | 1.85:1 | 3:1 以上 | 使わない(届かない) |
| (参考)バーガーの塗り: バンズ `#d8a45b` / 白い背景 | 2.24:1 | 3:1 | 参考(色だけに頼らない) |
| (参考)バーガーの塗り: パティ `#6b4432` / 白い背景 | 8.41:1 | 3:1 | 参考 |
| (参考)バーガーの塗り: レタス `#86b25a` / 白い背景 | 2.46:1 | 3:1 | 参考(色だけに頼らない) |
| (参考)バーガーの塗り: トマト `#d1533d` / 白い背景 | 4.19:1 | 3:1 | 参考 |
| (参考)バーガーの塗り: チーズ(マスタード黄) / 白い背景 | 1.85:1 | 3:1 | 参考(色だけに頼らない) |

- 実際の写真の上には、文字を置いていません(店名・評価は写真の下の白い地。仮の場所の「写真」の文字は、印です)。写真の上にあるのは、「審査待ち」の札(白い面・黒い文字)だけで、札の面が不透明なので、写真の明るさに左右されません
- バーガーの塗りの色は、白との比が 3:1 に届かないもの(バンズ・レタス・チーズ)があります。**部品の境目は、黒い輪郭(18.88:1)で伝え、塗りの色だけには頼りません**。塗ってある所と、水位より上(灰色の輪郭と白い面)の違いは、輪郭の色(黒 / 灰色)と面の色の両方で出し、数字が、必ずそばにあります
- 押せる部分は 44px 以上にしています(ボタン・入力欄・タブ・言語の切り替え・「詳しく見る →」などの行)。キーボードで選んだときは、黒の 2px の枠を、要素の 2px 外側に出す想定です(`値` ページに見本)

### 写真・文言・数字は、仮のものです

- **写真は、まだありません。** 写真の場所には、明るい茶色・緑・赤みのグラデーションと「写真」の文字を置いています。実際の写真が入ると、見た目の印象が変わるので、写真を入れた確認が、あとで必要です。グラデーションの向きは、Penpot と HTML で少し違います(仮の場所だけの差です)
- 文言は、日本語を案として書いています。**いまのアプリの画面は英語の表示**なので、日本語の文言は、アプリにまだありません(日本語と英語の切り替えは、アプリ側の別の作業です)
- 店名・レビューの文・ユーザー名は、開発用のサンプルデータ(`backend-go/cmd/seed`)に合わせています。ショップの一覧の「バーガーラボ 中目黒」「グリルバーガー 吉祥寺」と、評価・件数の数字は、見た目を確かめるための架空のものです

### いまの API にない情報(実装のときに必要になるもの)

見た目を作るために、いまのデータにないものを、次のように置いています。実装のとき、データを足すかどうかは、別に決めます。

- ショップの一覧の写真と、平均評価・件数(いまの一覧は、店名と状態だけです)
- レビューの詳細で、同じバーガーのほかのレビューを並べること(いまの詳細は、開いた 1 件だけです)
- 写真のない投稿の見せ方

### 実装のときの決め事(メモ)

- **評価のバーガーの水位は、`GET /meta` の評価の最大値(`rating.max`)から決めます**(ラフは 5)。水位は「表示する値 ÷ 最大値」で、フロントに 5 を書き写しません。最大値が 5 でなくなっても、水位はそのまま合います
- **水位は、数字と同じ丸め(1 桁)の値で決めます**(平均 4.27 は、数字が 4.3 なら、水位も 4.3 ÷ 最大値)。数字を丸めずに出して、水位だけ丸める、ということはしません
- バーガーの絵は、読み上げの文字(「5 段階中 4.5」)を持たせます。空・読み込み中の画面の絵は、飾りなので読み上げません
- **読み込み中は、水位が上がり続けるアニメーション**にするのを想定しています(ラフは、途中の 60% の静止画です)。動きを減らす設定(`prefers-reduced-motion`)のときは、止めます
- 投稿画面の評価の入力は、今回のラフの対象外です。作るときは、キーボードで操作できる部品(矢印キーで増減する選択肢)にして、数字を必ず表示します
- 一言の文言に置き換えるのは、**空・読み込み中・見つからない(404)だけ**です。API が返す検証・権限・通信のエラーの文言は、backend が決めるので、フロントは、そのまま出します(エラーの表示の枠は、見出しだけ画面側の言葉です)
- 赤は、削除とエラーだけです。あると良いもの: **削除の確認ダイアログ**(今回は作っていません)

### 作り方(作り直す手順)

「いまの画面のデザイン」と同じく、ヘッドレスのブラウザで HTML を開いて、位置・色・文字を一覧(JSON)にし、Penpot の API で登録しています(文字は編集できる文字のままです)。評価のバーガーは、`design/redesign/rating-icons.js` が、SVG のパスの一覧(部品ごとの、パス・塗り・輪郭の太さ)を作り、HTML に描き、同じ一覧を `extract.js` が JSON に入れ、`build_penpot.py` の `Design.icon` が、Penpot のパス図形(塗りは、単色か、境目が硬いグラデーション)にします。見本の HTML は `design/redesign/` にあります(`mock.css` が見た目、`shops.html`・`reviews.html`・`review-detail.html`・`states.html`・`tokens.html`・`rating-icons.html` が画面。言語は `?lang=en` で切り替え。バーガーは `mock.js` が `rating-icons.js` を使って、`data-score`・`data-size` から組みます)。値や配置を直したいときは、この HTML を直して、次の手順で作り直します。リポジトリのルートで実行します。

```bash
# 0. 作業用のディレクトリ(「いまの画面から作り直す」と同じ。Playwright を入れておく)
WORK=/tmp/penpot-work && mkdir -p $WORK && cp design/scripts/*.js design/scripts/*.py $WORK/
PW_IMAGE=mcr.microsoft.com/playwright:v1.50.1-jammy
docker run --rm -v $WORK:/work -w /work $PW_IMAGE npm i --no-save playwright@1.50.1

# 1. 見本の HTML を、位置・色・文字・パスの一覧(JSON)にする(12 画面と、値のページと、評価のバーガーの比較のページ。色の値とコントラストの計算結果も出す。評価のバーガーの自己確認もここ)
docker run --rm -v $WORK:/work -v "$PWD/design/redesign":/mock:ro -w /work -e OUT=/work/out-redesign $PW_IMAGE node capture_mock.js

# 2. Penpot を起動してユーザーを作り、パスワードを $WORK/penpot-pw.txt に 1 行で書く(「起動・停止・初期化」と「最初のユーザーを作る」)

# 3. Penpot に作る(数十秒。値のページと比較のページの画像用の行も、map-redesign.json に入る)
python3 $WORK/build_redesign.py --email <メール> --password-file $WORK/penpot-pw.txt --captures $WORK/out-redesign --map-out $WORK/map-redesign.json

# 4. 書き出す(「(参考)ブラウザを使わずに書き出す・読み込む」の export-binfile。ファイルの id は map-redesign.json の fileId)
#    → design/files/hamburger-evaluation-redesign.penpot に置く。ビューアーで開く前に書き出す

# 5. 画像にする
docker run --rm --network hamburger-penpot_penpot -v $WORK:/work -w /work \
  -e PENPOT_URL=http://localhost:9001 -e PENPOT_EMAIL=<メール> -e PENPOT_PASSWORD_FILE=/work/penpot-pw.txt \
  -e MAP_JSON=/work/map-redesign.json -e OUT=/work/render-redesign -e CLIP=1 -e QUALITY=72 $PW_IMAGE \
  sh -c 'node forward.js & sleep 1; node render_penpot.js'
```

- `CLIP=1` にすると、`render_penpot.js` が、画面の枠だけを切り出した JPEG を作ります(既定の PNG は、枠のまわりの余白つきです)。`tokens-pc.jpg` は `design/screenshots/redesign/values.jpg`、`rating-icons-pc.jpg` は `rating-icons.jpg` の名前で置いています。この 2 枚は大きい(1300〜1500px 幅 × 約 3000px)ので、`ONLY=tokens-pc,rating-icons-pc` と `QUALITY=48〜58` で撮り直して、300KB 前後にしています
- **確かめたこと**(今回の作り直し): (1) 書き出した `.penpot` を、新しいファイルとして読み込み直し、ページ 4 つ・図形の種類と数(パス 2,417 個を含む)・色・文字のスタイルが、元と同じだった。(2) 評価のバーガー 4 か所(大・中・小・空の画面)を、HTML の画像と Penpot の画像で、画素の差(3 つの色の値の絶対差の平均)を測った: 平均 4.0〜12.8(255 のうち)、64 を超えて違う画素は 0.15〜3.9%(線の縁と、曲線の描き方の違いによる)。(3) 目で、14 枚のうち 11 枚を見て(見ていないのは、ショップ一覧の日本語モバイルと英語 PC、状態の英語モバイルの 3 枚)、水位・輪郭・数字・赤の使い方に崩れがないことを確かめた。**文字の位置(24 か所)の画素比較は、前回の作り直しでは行いましたが、今回は行っていません**(文字の位置の登録の仕方は変えていません)
- 長方形の位置と大きさは、整数の px に丸めています(小数のままだと、暗い帯や輪郭の端に、細い線が出るため)。パス(バーガー)は、丸めていません
- `.penpot` の大きさは、約 3.8 MB(3,826,749 バイト)です。バーガーのパスが多いため(評価アイコンの比較のページだけで 1,216 個)で、前回(約 1.1 MB)より大きくなりました。画像は 14 枚で、1 枚あたり 40〜294 KB、合計約 1.3 MB です

### できていないこと・注意(このラフ)

- **部品(コンポーネント)にしていません。** ボタン・カード・札・バーガーなどは、同じ見た目の普通の図形です(バーガー 1 つは、大・中で約 6 個のパスとその細部(最大で全部で約 30 個)、小・極小で 3〜5 個のパスを入れたグループです)。決まった方向で画面を増やすときに、部品にします
- **状態は、画面 1 種類ずつです。** マウスを乗せた・押した・入力中などは、作っていません。空・読み込み中・見つからない・エラーは、文言の見本として、1 枚に 4 つを並べただけです(実際の画面の中での見え方は、別に決めます)。**読み込み中の水位のアニメーションは、静止画です**。キーボード操作の表示だけ、`値` ページに見本があります
- **配置は、位置の固定です。** Penpot の自動配置(Auto layout)や、大きさの追従は付けていません
- 実際の写真を入れた見え方・実機(iPhone など)で小さいバーガーが見分けやすいか・読み上げとキーボード操作は、確かめていません
- 残りの画面のうち、サインイン・新規登録・レビューの投稿と編集・ショップの詳細と追加は、次の「リデザインの画面(第 1 弾)」で作りました。プロフィール・管理・OAuth の許可の画面は、第 2 弾です

## ポートを変える

9001 が他のアプリと重なるときは、`design/.env` の `PENPOT_PORT` を変えて、`up -d` し直します(ブラウザで開く URL も、その値に追従します)。

## バージョンの更新

1. `design/.env.example` と `design/.env` の `PENPOT_VERSION` を、新しい安定版(<https://github.com/penpot/penpot/releases>)に変える
2. 更新する前に、開いている大事なファイルを `.penpot` に書き出しておく(バージョンをまたぐ互換のため)
3. `up -d` し直す(新しいイメージを取得して、データは残ったまま起動する)
4. 公式の compose の変更(`docker/images/docker-compose.yaml`)が、`design/docker-compose.yml` に必要か、差分を見る

## 公式の構成から外したもの

| もの | 理由 |
|---|---|
| `penpot-mcp` | エージェントが Penpot を読む仕組み。この story の対象外(必要になったら別の story)。外しても frontend は起動する(確認済み) |
| `penpot-mailcatch` と `enable-smtp` | メールは送らない。SMTP を有効にしない Penpot は、メールの内容をログに出すだけ。メールの確認もしない(`disable-email-verification`) |
| テレメトリ | 匿名の利用データを送らない(`PENPOT_TELEMETRY_ENABLED=false`) |
| `restart: always` | Docker の起動で勝手に立ち上がらないようにした |

## 動作の目安(実測)

測定: Penpot 2.17.2、Docker Desktop の VM(4 CPU・7.7 GiB)、Apple Silicon(arm64)。

| 項目 | 値 |
|---|---|
| イメージの合計(ディスク) | 約 4.0 GiB(exporter 2.17 GB・backend 739 MB・frontend 540 MB・postgres 468 MB・valkey 142 MB) |
| 起動時間(イメージ取得済み、空の volume から) | frontend が応答するまで 約 10 秒、backend の API が応答するまで 約 20〜25 秒(実測 19 秒・25 秒) |
| 初回のイメージの取得 | 数分(回線による) |
| メモリ(使用中) | 合計 約 1.2 GiB(backend 約 780 MiB・exporter 約 170 MiB・frontend 約 115 MiB・postgres 約 135 MiB・valkey 約 10 MiB) |
| メモリの上限(`mem_limit` の合計) | 3.2 GiB(frontend 192 MiB・backend 1.5 GiB・exporter 768 MiB・postgres 512 MiB・valkey 256 MiB) |

## トラブルシュート

- `PENPOT_SECRET_KEY` が未設定のエラー → 「初回セットアップ」の 2 を実行する
- `create-profile` で `No such container` → `docker ps` で、名前が `<プロジェクト名>-penpot-backend-1` のコンテナを確認する
- メモリ不足で落ちる → 他のコンテナを止める。それでも足りなければ `design/docker-compose.yml` の `mem_limit` を上げる

## リデザインの画面(第 1 弾: サインイン・レビューの投稿と編集・ショップ)

ラフ(上の節)で決まった方向で、残りの画面のうち**第 1 弾**を、同じファイル(`design/files/hamburger-evaluation-redesign.penpot`)に足しました。ラフで作った 6 画面と `値`・`評価アイコンの比較` のページは、そのままです(作り直したときに、同じ見本から同じものができます)。**画面は 20 種類(状態違いを含む)で、日本語・英語、PC・モバイルの 4 通りを作ったので、枠は 80 個**です。決まった方向は変えていません(白い背景に黒い文字・マスタードの黄色は塗りだけ・赤は削除とエラーだけ・水位で塗るバーガーの評価・一言の文言は空と読み込み中と見つからないときだけ・PC 1280px とモバイル 375px・WCAG AA)。

### 画面の一覧

Penpot のページは、グループ × PC / モバイルで分けています(上の PAGES の表)。日本語の枠が上の行、英語の枠(名前の末尾に「(English)」)が下の行です。画像は `design/screenshots/redesign/` に、`<キー>-pc.jpg`・`<キー>-mobile.jpg`(日本語)、`<キー>-en-pc.jpg`・`<キー>-en-mobile.jpg`(英語)の名前で置いています。

| グループ(ページ) | 画面 | 枠の名前(PC / モバイル) | キー |
|---|---|---|---|
| 認証 | サインイン | `サインイン / PC` | `signin` |
| 認証 | サインイン(エラー) | `サインイン(エラー) / PC` | `signin-error` |
| 認証 | 新規登録 | `新規登録 / PC` | `signup` |
| 認証 | 新規登録(エラー) | `新規登録(エラー) / PC` | `signup-error` |
| 認証 | 確認メールを送った | `確認メールを送った / PC` | `signup-sent` |
| 認証 | メールの確認(確認中) | `メールの確認(確認中) / PC` | `confirm-loading` |
| 認証 | メールの確認(完了) | `メールの確認(完了) / PC` | `confirm-done` |
| 認証 | メールの確認(期限切れ・無効) | `メールの確認(期限切れ・無効) / PC` | `confirm-error` |
| レビューの投稿・編集 | レビュー投稿(最初の状態) | `レビュー投稿 / PC` | `review-new` |
| レビューの投稿・編集 | レビュー投稿(入力済み) | `レビュー投稿(入力済み) / PC` | `review-new-filled` |
| レビューの投稿・編集 | レビュー投稿(エラー) | `レビュー投稿(エラー) / PC` | `review-new-error` |
| レビューの投稿・編集 | 写真の欄の状態(なし・小さくしている途中・付いた・送信中・エラー 4 つ) | `写真の欄の状態 / PC` | `review-photo` |
| レビューの投稿・編集 | レビュー編集 | `レビュー編集 / PC` | `review-edit` |
| レビューの投稿・編集 | レビュー編集(編集できない) | `レビュー編集(編集できない) / PC` | `review-edit-forbidden` |
| レビューの投稿・編集 | 削除の確認 | `削除の確認 / PC` | `review-delete` |
| ショップ | ショップ詳細 | `ショップ詳細 / PC` | `shop-detail` |
| ショップ | ショップ詳細(審査待ち) | `ショップ詳細(審査待ち) / PC` | `shop-detail-pending` |
| ショップ | ショップ詳細(却下) | `ショップ詳細(却下) / PC` | `shop-detail-rejected` |
| ショップ | ショップ追加 | `ショップ追加 / PC` | `shop-new` |
| ショップ | ショップ追加(エラー) | `ショップ追加(エラー) / PC` | `shop-new-error` |

各画面(日本語)の下には、灰色の「**実装メモ**」の箱があります。その画面の値がどこから来るか(API か、画面側か)、frontend に規則を書き写さない点、実装のとき決めることを書いています。**アプリの見た目ではなく、デザインへの注記**です(英語の画面には付けていません)。

### 案の中身

- **サインイン・新規登録**: PC は、左に大きな写真、右にフォームの 2 列です。モバイルは、フォームだけです(写真は出しません)。ヘッダーは、サインインしていない人向けに、「サインイン」「新規登録」が並びます。入力欄は 44px 以上の高さで、ラベルは入力欄の上、補足はその下です。エラーは、赤い枠と帯・「!」・見出し・**API が返した文言(英語のまま)**で、複数あれば「・」で並べます。新規登録の「確認メールを送りました」は、送り先のメールアドレスを太字にして、届かないときの案内と、「もう一度、新規登録する」ボタンを置いています
- **メールの確認(リンクから開く画面)**: 中央に、確認中(水位が途中のバーガー・「メールアドレスを確認しています…」)、完了(「メールアドレスを確認できました」・一覧へ進む案内)、期限切れ・無効(赤いエラー枠に API の文言 `Confirmation token is invalid or has expired`・「もう一度、新規登録する」)を出します
- **レビューの投稿・編集**: PC は、左に入力(バーガーの名前・評価・コメント)、右に写真の 2 列です。モバイルは 1 列(名前 → 評価 → コメント → 写真 → ボタン)です。**評価の入力**は、水位のバーガーと、選んだ数字(大きく)と、**数字のボタン 1〜5(ラジオボタンの集まり)**です。押せるのは数字のボタンで(44px 以上)、選んだボタンは黒い面に白い文字にし、バーガーの水位が、選んだ値に合わせて変わります。**写真の欄**は、「なし(写真を選ぶボタン)」「大きい写真を、この端末で小さくしている途中(進行の棒と、大きさの変化)」「付いた(見本・ファイル名・大きさ・写真を変える/外すボタン)」「送信中(押せないボタン)」があり、**保存できなかったときの API の文言 4 つ**(大きすぎる・形式が対応外か壊れている・iPhone の HEIC/HEIF・縦横が大きすぎる)を、赤いエラー枠で見せます(`写真の欄の状態` の画面)。編集は、評価とコメントと写真を直す形で、他人のレビューでは、フォームを出さず、「このレビューは、編集できません」を出します
- **削除の確認**: レビューの詳細の上に、後ろを暗くしたダイアログを出します(「このレビューを削除しますか?」「削除すると、元に戻せません。」・「キャンセル」・赤い面に白い文字の「削除する」)。いまのアプリは、ブラウザ標準の確認です。あると良いもの、として作りました
- **ショップの詳細**: 写真(見本)→ 店名(と、審査待ち・却下の札)→ 平均のバーガーと件数と「レビューを書く」→ レビューのカード(写真・バーガーの名前・評価・コメント・投稿した人)を、PC は 2 列、モバイルは 1 列で並べます。審査待ちは、写真の代わりに「写真はまだありません」・札「審査待ち」・注意の箱(「このショップは、審査中で、まだ公開されていません。」)・レビューがないときの「まだ誰も食べていません」を出します。却下は、札「却下」と、理由つきの注意の箱で、「レビューを書く」は出しません
- **ショップの追加**: 店名の入力と、審査の案内(注意の箱)と、「審査に出す」「キャンセル」です。エラーは、API の文言(`Name can't be blank`)をそのまま、赤いエラー枠で出します

### この弾で足した部品(新しい色は足していません)

入力欄のラベル・補足・文字数のカウンター、複数行の入力欄、評価の入力(水位のバーガー + 数字のボタン)、写真の欄(なし・進行・付いた)、進行の棒、注意の箱、押せないボタン、確認のダイアログ(暗幕つき)、赤い面の「削除する」ボタン、サインインしていない人のヘッダーです。色は、`値` ページの 15 色のままです。

### 新しく測ったコントラスト(WCAG AA。`値` ページの表と同じ)

| 組み合わせ(前景 / 背景) | 比率 | 基準 | 結果 |
|---|---|---|---|
| 削除の確認の「削除する」ボタンの白い文字 / 赤の面 | 6.54:1 | 4.5:1 以上 | ○ |
| 押せないボタン(送っている間・小さくしている間)の文字 / 薄い灰色の面 | 6.19:1 | 4.5:1 以上 | ○ |

そのほかの組み合わせ(黒い文字と白い背景・補足の文字・入力欄の枠・黄色の面の上の黒い文字・エラーの赤)は、上の表と同じ色の組み合わせです。数字のボタンで選んだ側(白い文字 / 黒い面)は、言語切替と同じ 18.88:1 です。

### 実装のときの決め事(第 1 弾のメモ)

- **文字数の上限(コメント・バーガーの名前など)は、`GET /meta` の `text` から取ります**(frontend に数字を書きません。数え方はコードポイント数)。カウンターの部品(`CharCounter`)は実装済みで、上限を超えても入力・送信を止めず、判定は backend の 422 に任せます。見本の数字は仮です
- **パスワードの説明文の数字(最小・最大のバイト数)は、`GET /meta` の `password` から取ります**(frontend の文言には数字を書きません)
- 評価の入力の段階の数と水位は、`GET /meta` の評価の範囲から決めます(frontend に 5 を書きません)。最初に選ばれている値(いまのアプリは 3)は、実装のときに決めます。矢印キーで増減できるラジオボタンの集まりにし、選んだ数字を必ず表示します
- 写真は、長辺かファイルの大きさが上限を超えたときだけ、この端末で小さくしてから送ります。上限は `GET /meta` の `photo` から取ります。**保存できなかったときの写真の文言(英語)は、backend が返したものをそのまま出します**(frontend で書き換えません)。API のエラーは、どの入力欄のものか分からない形なので、画面の上にまとめて出します(見出しだけが、画面側の言葉)
- 「メールの確認」の完了の画面は、確認できてから一覧へ進むまでの一瞬の画面です(いまのアプリは、確認できたらすぐ一覧へ進みます)。確認中の文言は、遊びの文言(「焼いています…」)ではなく、内容が分かる「メールアドレスを確認しています…」にしました(空・読み込み中・見つからないときの遊びの文言は、一覧などの読み込みに使います)
- 「レビューを書く」「編集」を出すかは、backend が返す `can_review`・`can_edit` で決めます。ショップの写真・平均のバーガー・件数は、いまの API にありません(ラフと同じ前提です)
- 削除の確認は、いまブラウザ標準の確認です。この案(専用のダイアログ)にするかは、実装のときに決めます

### 作り方の変更(作り直す手順)

手順は、上の「作り方(作り直す手順)」と同じです。変わったのは次の点です。

- 見本の HTML は、`?state=` で状態を切り替えます(例: `signin.html?state=error`)。`data-only="error"` のように書いた要素は、その状態のときだけ残ります(`mock.js`)。ヘッダーは、`<body data-auth="out">` のとき、サインインしていない人向けになります
- `capture_mock.js` の `GROUPED` に、画面(グループ・キー・名前・HTML・状態)を並べています。日本語と英語は、自動で作ります。書き出す JSON の一覧(`screens.json`)を、`build_redesign.py` が読んで、グループごと・PC / モバイルごとに、Penpot のページを作ります
- 画像にするときは、`ONLY` に、`signin-pc,signin-en-mobile` のように、キーと大きさをつなげて並べます(英語はキーの末尾に `-en`)
- 見本の HTML の箇条書きの「・」は、文字として書いています(Penpot に写すとき、リストの印は、文字ではないので落ちるため)

### この弾で、確かめたこと・確かめていないこと

- **確かめたこと**: (1) 書き出した `.penpot` を、新しいファイルとして読み込み直し、ページ 10 個と、図形の種類と数(ページごと)が、元と同じだった。(2) Penpot の閲覧画面を画像にして、HTML の画像と目で比べた(日本語・英語の PC・モバイルのうち、レビューの投稿・写真の欄の状態・ショップの詳細・新規登録・メールの確認などを見た。全 80 枚を 1 枚ずつは見ていません)。(3) 新しい 2 つの組み合わせのコントラストを実測した
- **確かめていないこと**: 文字の位置の画素比較(前回のラフでは行いましたが、今回は行っていません)、Penpot の編集画面での確認、実際の写真を入れた見え方、実機での見え方(iPhone など)、読み上げとキーボード操作(評価の数字のボタンの矢印キーの動きも、デザインの段階の想定です)
- `.penpot` は、約 8.9 MB(8,871,867 バイト)になりました(前回は約 3.8 MB)。枠が 80 個増え、日本語と英語が両方あるためです。第 2 弾で、さらに増える見込みです
- 画像は 80 枚(1 枚あたり 約 25〜132 KB、合計 約 3.9 MB)と、更新した `values.jpg`(約 271 KB。新しい 2 つのコントラストを足したため)です。ラフの 12 枚と `rating-icons.jpg` は、変えていません

## リデザインの画面(第 2 弾: プロフィール・管理・アプリの接続・見え方の一覧)

第 1 弾までの方向(白い背景に黒い文字・マスタードの黄色は塗りだけ・赤は削除とエラーだけ・水位で塗るバーガーの評価・一言の文言は空と読み込み中と見つからないときだけ・PC 1280px とモバイル 375px・WCAG AA)のまま、残りの画面を、同じファイル(`design/files/hamburger-evaluation-redesign.penpot`)に足しました。**日本語 26 種類(状態違いを含む)と、英語 6 種類を、PC・モバイルで作ったので、枠は 64 個増えました**(英語は、主な画面だけです。状態や変種の見本は、日本語だけ)。あわせて、**「レビューの編集ボタンなどは、権限によって表示・非表示が変わるのでは?」という指摘**に答えるため、「見え方の一覧」のページと、見え方が違う画面の変種を作りました。

### 権限による見え方(利用者の指摘への答え)

そのとおりです。実装では、backend が返す項目で、frontend が出し分けています。**第 1 弾までの画面は、その違いを例で見せただけで、明示していませんでした**。今回、`backend-go` の `domain` の規則と `frontend/src` の出し分けを読んで、実際の項目名と意味を確かめ、一覧にしました(`見え方(PC)`・`(モバイル)` ページの `見え方の一覧`)。要点:

| 部品 | 決める API の項目 | 出る人 |
|---|---|---|
| レビューの「編集」「削除」 | `can_edit`(レビューの応答) | 投稿した本人だけ。**管理者でも、他人のレビューは変えられない**(domain に例外がない) |
| ショップの「レビューを書く」 | `can_review`(ショップの詳細の応答) | 公開中: サインインした人なら誰でも。審査待ち: 作った本人と管理者だけ。却下: 誰にも出ない。サインインしていない人には出ない |
| 一覧に並ぶ・開けるショップ | API が絞って返す(見えないショップは 404) | サインインしていない人・他人: 公開中だけ。作った本人: 公開中と自分のもの。管理者: すべて |
| 「審査待ち」「却下」の札と理由 | `status`・`moderation_note` | 作った本人と管理者だけ(それ以外の人には、そのショップ自体が返らない) |
| 「ショップを管理」・管理の画面 | `can_moderate`(`GET /me`) | 管理者だけ。サインインしていない人はサインイン画面へ、管理者でない人はショップの一覧へ戻す |
| 「承認する」「却下する」 | `can_approve`(公開中でないとき)・`can_reject`(却下でないとき) | 管理の画面の中で、ショップごとに backend が返す |
| 「プロフィールを編集」・アカウントの削除・「接続済みのアプリ」 | `can_edit`(ユーザーの応答) | 本人だけ。**管理者でも、他人のプロフィールは変えられない** |
| メールアドレスの表示 | API が本人の閲覧のときだけ返す(`email`) | 本人だけ |
| 「ショップを追加」・ヘッダーのリンク・サインイン画面 | サインインしているか(API の項目ではない) | サインインの状態で決める |

- 権限の判断は backend の `domain` だけが持ち、**frontend は、返された項目で出し分けるだけ**です(投稿者や管理者かどうかを frontend が比べて決めません)。値が `false` の部品は、押せない状態で見せず、出しません
- 画面の変種(`見え方` ページ): レビュー詳細(他人のレビュー・サインインしていない)、ショップ詳細(サインインしていない)、ショップの一覧(サインインしていない・管理者)。第 1 弾の画面(レビュー詳細・ショップの一覧・ショップの詳細・サインイン)の実装メモにも、この説明を足しました(見た目は変えていません)
- **第 1 弾の実装メモの訂正**: ショップの詳細の「審査中は、作った本人だけに出る」は、正しくは「**作った本人と管理者だけ**」でした(`domain.Shop.CanBeReviewedBy`)。メモと、審査待ちの注意の文を直しました
- **レビュー詳細の「レビューを書く」は、いまの API では出し分けられません**。レビューの応答に `can_review` がなく(ショップの詳細にはあります)、そのショップに書けるかが分かりません。実装するなら、レビューの応答に足すか、このボタンをレビュー詳細に置かないかを決めます

### 画面の一覧

Penpot のページは、グループ × PC / モバイルで分けています。画像は `design/screenshots/redesign/` に、`<キー>-pc.jpg`・`<キー>-mobile.jpg`(日本語)、`<キー>-en-pc.jpg`・`<キー>-en-mobile.jpg`(英語)の名前で置いています。

| グループ(ページ) | 画面 | キー | 英語 |
|---|---|---|---|
| プロフィール | 自分のプロフィール(名前・自己紹介・メール・リンクのコピー・プロフィールを編集・接続済みのアプリ・レビュー) | `profile` | あり |
| プロフィール | 他のユーザーのプロフィール(メールと編集と接続済みのアプリは出ない) | `profile-other` | あり |
| プロフィール | 自分のプロフィール(レビューも接続もない) | `profile-empty` | |
| プロフィール | プロフィールの状態(リンクをコピーした・コピーできなかった・接続済みのアプリの読み込み中・読み込めなかった・解除の確認・解除している・解除できなかった・接続がない) | `profile-states` | |
| プロフィール | プロフィールの編集(通常・エラー・アカウント削除の確認・編集できない) | `profile-edit`・`profile-edit-error`・`profile-delete`・`profile-edit-forbidden` | 通常のみ |
| 管理 | ショップの管理(すべて。審査待ち・公開中・却下の 3 つの行) | `admin` | あり |
| 管理 | ショップの管理(却下の理由を書く・却下に失敗・審査待ちがない) | `admin-reject`・`admin-error`・`admin-empty` | |
| 管理 | ショップの編集(名前だけ。通常・エラー) | `admin-edit`・`admin-edit-error` | |
| アプリの接続 | 許可の画面(読み取りだけ・読み取りと書き込み) | `oauth-consent`・`oauth-consent-write` | あり |
| アプリの接続 | 許可の画面(確認中・接続中・エラー) | `oauth-consent-loading`・`oauth-consent-connecting`・`oauth-consent-error` | |
| アプリの接続 | サインイン(アプリの接続の途中) | `oauth-signin` | |
| 見え方 | 見え方の一覧 | `visibility` | |
| 見え方 | 変種: レビュー詳細(他人のレビュー・サインインしていない)・ショップ詳細(サインインしていない)・ショップの一覧(サインインしていない・管理者) | `vis-review-other`・`vis-review-out`・`vis-shop-out`・`vis-shops-out`・`vis-shops-admin` | |

### 案の中身

- **プロフィール**: 大きな丸(頭文字)・名前・自己紹介・(本人だけ)メールアドレス・「リンクをコピー」(誰にでも)・「プロフィールを編集」(本人だけ)。本人の画面には、続けて「接続済みのアプリ」(アプリの名前・許可した範囲の説明・接続した日・**赤い枠の「接続を解除」**)と「もっと見る」、そしてレビューのカード(写真・バーガーの名前・水位のバーガー・コメント・お店の名前)が並びます。他のユーザーの画面には、接続済みのアプリを出しません。レビューが 1 件もないときは、空の一言(「まだ誰も食べていません」)です
- **プロフィールの編集**: ユーザー名・自己紹介・メールアドレス・新しいパスワード(空のままなら変えない)・確認と、「変更を保存する」「キャンセル」、区切り線の下に「アカウントの削除」(**赤い枠のボタン**。押すと、赤い面の「削除する」がある確認のダイアログ)。エラーは、API の文言をそのまま並べる赤いエラー枠です。他人のプロフィールの編集ページを開いたときは、フォームを出さず、「編集できません」だけです
- **管理**: 絞り込み(すべて・審査待ち・公開中・却下。選んでいるものは黒い面)と、ショップの行(名前・状態の札・作った人・却下の理由・操作)。操作は、審査待ち: 編集・承認する(黄色)・却下する、公開中: 編集・却下する、却下: 編集・承認する(`can_approve`・`can_reject` のとおり)。**「却下を確定する」は黒い面のボタン**です(赤は、削除とエラーだけなので、却下の操作に赤は使いません)
- **アプリの接続(許可の画面)**: **「何のアプリに、何を許すか」を、いちばん目立たせます**。見出しの下に、黒い太い枠の面で、アプリの名前(大きな文字)とアプリ ID を置き、その下に「許可すること」(API が返した範囲の説明をそのまま並べる)、戻り先のホストとサインインしている名前、「許可する」(黄色)・「許可しない」(白い面に枠)の順です。確認中・接続中の画面(水位が途中のバーガー)と、エラー(API の文言 `oauth authorization request is invalid`)も作りました
- **サインイン(アプリの接続の途中)**: サインインしていない人が許可の画面へ来たときに、サインイン画面の上に一言(案。いまのアプリは何も出さない)を足したものです

### 新しく測ったコントラスト

**新しい色の組み合わせはありません**(第 2 弾で使った組み合わせは、すべて `値` ページの表に、すでにあります: 黒い文字 / 白・薄い灰色の面、補足の文字 / 白・薄い灰色の面、黒い文字 / 黄色の面、赤い枠と文字 / 白い面、赤いエラー、白い文字 / 黒い面(「却下を確定する」と選んだ絞り込み)、白い文字 / 赤い面(「削除する」))。見え方の一覧の「出ない」は、補足の色(6.93:1)で書いていますが、**言葉(「出ない」)でも伝えている**ので、色だけに頼っていません。

### 実装のときの決め事(第 2 弾のメモ。各画面の「実装メモ」にも書いています)

- **API が言語に合わせて文言を返しません**。許可の範囲の説明(「ショップ・レビュー・プロフィールを読む」など)と、API のエラー文言(英語)は、日本語の画面でも英語の画面でも、返された言語のままです。言語の切り替えを実装するときに、API 側の対応(言語を指定して返す、または種類のコードを返して frontend が言語ごとの文言にする)を決めます
- **文字数のカウンター(自己紹介 500・ショップの名前 100)は、いまの `GET /meta` にありません**。第 1 弾のコメント・バーガーの名前と同じ扱いです。パスワードの説明文の「8〜72 バイト」も、frontend に書き写した数字です
- **許可の画面で、書き込みの範囲を強調するには、範囲ごとの印(例: 変更する範囲かどうか)を API が返す必要があります**(いまは名前と説明だけ。frontend が名前を比べて決めると、規則の複製になります)
- 削除の確認(レビュー・アカウント)と、接続の解除の確認は、いまのアプリでは、ブラウザ標準の確認です。すべて、同じ形の専用のダイアログにする案です
- ショップの一覧の写真と平均のバーガー・件数、レビュー詳細の「レビューを書く」の出し分け(`can_review` が応答にない)は、いまの API にありません

### 作り方の変更(第 2 弾)

- 見本の HTML は、`?auth=out`(サインインしていない人のヘッダー)・`?user=bob`(ヘッダーのユーザー名)・`?active=none`(どのタブも選ばない)・`?active=me`(ユーザー名のタブを選ぶ)でも、見え方を切り替えられます(`mock.js`)。権限で見え方が変わる画面(`見え方` ページ)を、同じ HTML から作るためです
- `capture_mock.js` の `GROUPED` の各行は、`[グループ, キー, 画面の名前, HTML, 状態, 追加の query, 言語]` です。追加の query は、上の `auth=out&user=bob` のような文字列(なければ `null`)、言語は `['ja']`(日本語だけ)のように絞れます(なければ日本語と英語の両方)
- **複数行の入力欄は、`<textarea>` ではなく、`<div class="input area" role="textbox">` で書きます**(第 1 弾と同じ)。`<textarea>` の文字は、Penpot に写したとき、縦の中央に寄ってしまうためです
- 画像は、作り直した画面のうち、**見た目が変わったものだけ**を撮り直しました(見本の HTML の位置・文字・色を、変更前と変更後で比べて決めました)。第 1 弾までの画面のうち、見た目が変わったのは 23 枚で、第 1 弾の実装メモを足した画面(レビュー詳細・ショップの詳細・ショップの一覧・サインイン)と、モバイルで左右の余白が消えていた「メールの確認」「レビューの削除の確認」などです(**幅の指定が余白を打ち消していた不具合を直しました**)。それ以外は、変えていません
- Penpot の閲覧画面が読み込み中のまま撮れた画像(暗い画像)は、画像の平均の明るさで見つけて、撮り直しました(削除の確認は、暗幕のため暗いのが正しい)

### この弾で、確かめたこと・確かめていないこと

- **確かめたこと**: (1) 書き出した `.penpot` を、新しいファイルとして読み込み直し、**ページ 18 個**と、図形の種類と数(ページごと)が、元と同じだった。(2) Penpot の閲覧画面の画像を、目で確かめた(プロフィール(日本語・英語)・プロフィールの編集(エラー・削除の確認)・プロフィールの状態・管理の一覧(日本語・英語・却下の理由・審査待ちなし)・許可の画面(英語・エラー)・サインイン(アプリの接続の途中)・ショップの詳細(サインインしていない)・レビュー詳細の変種(他人のレビュー)・見え方の一覧を見た。**全 87 枚を 1 枚ずつは見ていません**。見つけた不具合(複数行の入力欄の文字が縦の中央に寄る・英語の「Reason:」の区切りがない・モバイルの案内文の折り返し・モバイルで左右の余白がない)は、直して撮り直した)。(3) 権限の項目と規則は、`backend-go` の `domain`・`handler` と `frontend/src` を読んで確かめた(推測で書いていません)。(4) 撮り直した画像は、明るさで、読み込み中のまま撮れたものがないことを確かめた
- **確かめていないこと**: 文字の位置の画素比較、Penpot の編集画面での確認、実際の写真を入れた見え方、実機での見え方(iPhone など)、読み上げとキーボード操作、見え方の一覧の各行を実際のアプリで 1 つずつ操作して確かめること(規則は、コードから起こしました)
- `.penpot` は、約 15.3 MB(15,310,425 バイト)になりました(第 1 弾の後は約 8.9 MB)。枠が 64 個増えたのと、水位のバーガーのパスが多いためです。git の履歴には、更新のたびに全体が入るので、これ以上増やすときは、ファイルの分け方(例: 画面ごと)を考えたほうがよいです
- 新しい画像は 64 枚(1 枚あたり 約 15〜216 KB、合計 約 4.8 MB)、撮り直した画像は 23 枚(合計 約 1.4 MB)です

## ロゴの案(BurgerStack)

アプリの名前は「BurgerStack」に決まりました(ハンバーガーが積み上がっていく感じ)。ロゴの 3 案を、`redesign/logo-variants.html` に作り、比較の 1 枚を `screenshots/redesign/logo-variants.jpg` に置いています。**案は、まだ選ばれていません。**

| 案 | 中身 | 強み | 弱み |
|---|---|---|---|
| 1 文字だけ | Burger は黒、Stack は黄色の札 | いまの作りを引き継ぎ、いちばん軽い | 名前の意味も、バーガーも、絵では伝わらない |
| 2 記号 + 文字 | 積み上がった 4 段の記号を文字の左に置く | 記号だけでファビコンにできる。評価のバーガーと系統が揃う | 記号の画像が要る。16px では 4 段がつぶれ気味 |
| 3 文字が積み上がる | Burger が上のバンズ、Stack がパティになる 2 段組 | 名前の意味がいちばん伝わる | 小さい所で読めず(高さ 48px 以上が要る)、横並びの別版も要る |

- 比較の見方: 各案を、(a) ヘッダー、(b) 大きく、(c) 小さく(高さ 20px)、(d) 白黒、(e) ファビコン(32px・16px)、(f) コントラスト比(実測)、(g) 日本語の画面、の順に並べています。コントラスト比は、ページを開いたときに、色の値から計算しています(WCAG の相対輝度)。
- 色は、これまでの白・黒・グレー・マスタード黄だけです。バーガーの部品の色は、記号(案 2・3)にだけ使っています。書体は、いまの画面と同じ Noto Sans JP です。
- 4 つ目の案は、足していません(3 案で比較に足りるため)。
- **選ばれたあとの作業**: (1) 全画面のヘッダーの「Hamburger Evaluation」を、選ばれたロゴに差し替える(`redesign/*.html` の `.wordmark`)、(2) 画像を撮り直す、(3) `.penpot`(約 15 MB のバイナリ。更新のたびに、履歴へ全体が入る)を、**1 回だけ**更新する。この PR では、`.penpot` を更新していません。

### ロゴ: 案 2 が選ばれ、記号をシンプルにする派生の案(2a・2b・2c)

利用者が案 2(記号 + 文字)を選び、「記号をもっとシンプルにして欲しい」と要望しました。記号だけを変えた派生の案を、`redesign/logo-variants.html` の後半に足し、比較の 1 枚を `screenshots/redesign/logo-simple.jpg` に置いています(前回の 3 案は、そのまま残しています)。**派生の案も、まだ選ばれていません。** 文字の部分(Burger は太字、Stack は標準の太さ)は、案 2 のままです。

| 案 | 記号 | 16px での読みやすさ | 弱点 |
|---|---|---|---|
| 2a 3 段 | バンズ(黄)・パティ(黒のベタ)・バンズ(黄)。細い黒の輪郭 | 色の違いで 3 段は数えられるが、輪郭がぼやけ、すき間が灰色の帯になる | 評価のバーガーと、形が似ていて、紛らわしさが残る |
| 2b 黒の棒を積む | 黒のベタの丸い棒 3 本と、間にはさんだ黄色の細い線(チーズ) | 3 本の棒がはっきり読める。黄色の線は 1 画素の細い線になるが見える | バーガーだと分かる手がかりが弱い(黄色の 1 本と、パティの棒が少し広いだけ) |
| 2c 丸い帯を積む | 同じ大きさの丸い帯 3 段。中の 1 段だけ黄色。黒の輪郭 | 3 段は数えられるが、輪郭が灰色にかすみ、3 案のなかでいちばん弱い | 名前の Burger との結びつきが弱い |

- 前回の案 2(4 段)は、16px で、輪郭とすき間が混ざり、4 段がつぶれ気味でした。3 案とも、段が 3 つ以下に減っています。
- 色は、白・黒・マスタード黄だけです(バンズやパティの茶色は使っていません)。黄色は、白い地に 1.85:1 で、境界に頼れないので、2a・2c は黒の輪郭で、2b は黒の棒にはさむ形で補っています。
- ファビコンは、記号を、白い丸い角の下地に置いています(暗いタブでも、黒の輪郭が消えないように)。
- 小さい所の見え方は、実寸の 20px・16px を撮り、画素を 8 倍に拡大して並べています。
- 私の推奨は **2b** です(いちばんシンプルで、16px でも読め、評価のバーガーと絵の系統が違う)。バーガーらしさを残したいなら 2a です。
- 形が確定したあとの作業は、前の節のとおりです(全画面のヘッダーの差し替えと、`.penpot` の更新を、1 回だけ)。

### ロゴ: 一筆書きの黄色い線で描く案(1a・1b・1c・1d)

利用者の要望「ロゴを、一筆書きの黄色い線で描けませんか」への案です。`redesign/logo-variants.html` の末尾に足し、比較の 1 枚を `screenshots/redesign/logo-oneline.jpg` に置いています(前の案は、そのまま残しています。**まだ選ばれていません**)。ペンを離さない 1 本の連続した線(丸い端・一定の太さ・マスタード黄)で描き、文字の部分は案 2 のままです。

| 案 | 中身 | 強み | 弱み |
|---|---|---|---|
| 1a 蛇行する積み重ね | 右へ(下のバンズ)→ 左へ(パティ)→ 右へ(波)→ 左へ、上にドーム。全体が S の字にも見える | 積み重ねに見える。評価のバーガーと絵の系統が違う。1 本の path で軽い | バーガーの手がかりは、ドームと波だけ |
| 1b バーガーをなぞる線 | ドーム → レタスの波 → パティ → 下のバンズを、なぞる 1 本線 | バーガーだと、いちばんはっきり分かる。16px でも読める | 評価のバーガーと形が似て紛らわしい。一筆書きの流れが見えにくい |
| 1c 下線へつながる線 | 記号を描いた線が、そのまま「BurgerStack」の下線へ流れる。**記号は、1a を左右反転した向き**(始まりが下線の右端で、折り返しの側が 1a と反対) | 一筆書きの意味が、いちばん伝わる | 文字の幅を測って下線を通すので実装が重い。20px 以下で下線が消えかかる |

- **黄色い線の見えにくさ**: 黄色は、白地に対して 1.85:1 で、線としては見えにくいです。そこで、各案を 3 つの置き方で並べました。(i) **黒い札の上**(黄と黒は 10.20:1。16px まで確実に読める)、(ii) 白地に、**黄 + 細い黒の縁取り**(16px で線の間が埋まってにじむ。一筆書きの雰囲気も崩れる)、(iii) 白地に、**太い黄色だけ**(実測 1.85:1。20px 以下では、とても淡い)。
- 16px での読みやすさ: (i) は、1a・1b・1c・1d とも読めます(1a と 1d は左右反転なので、読みやすさは同じ。波は点線のようにつぶれ気味。1b はバーガーだと分かる)。(ii) はにじみ、(iii) は淡くなります。
- 私の推奨は **1a を (i) 黒い札の上に置く形**です(理由と代償は、比較の画像の最後に書いています。代償: ヘッダーに黒い札が入り、いまの画面の中では、いちばん濃い部分になります)。それが強すぎるなら、前回の 2b が次の候補です。
- **オプション(作っていません)**: 一筆書きなので、サインイン画面などで、線が「描かれていく」動きを付けられます(`stroke-dasharray` と `stroke-dashoffset`。path に `pathLength="1"` を付ければ、長さを測らずに済む)。`prefers-reduced-motion` のときは動かさず、描き終えた形で出します。CSS 数行で軽い一方、丸い端が描き始めに点として見えること、ページを移るたびに動かすとうるさいこと(1 回だけにする)に注意が要ります。
- 形が確定したあとの作業は、前の節のとおりです(全画面のヘッダーの差し替えと、`.penpot` の更新を、1 回だけ)。`.penpot` は、この PR でも更新していません。

#### 訂正と追加: 1c の記号の向き、1d(1c の向きで、下線なし)

- **訂正**: 前の版(PR #121)の説明や報告に、「1c の記号は 1a と同じ」と書いた所がありましたが、**誤りでした**。1c の記号は、1a を左右反転した向きです(始まりが下線の右端で、折り返しの側が 1a と反対)。1c のファビコンが、しっぽなしの 1a の形になっていた食い違いも、直しました(1c のファビコンは、1c の向きで、短いしっぽつき)。
- **1d(1c の向きで、下線なし)**: 1c の記号の形をそのまま使い、下線としっぽを取った案を足しました。`logo-variants.html` の先頭に、1a と 1d を、同じ大きさ・同じ札・同じ線の太さで、左右に並べた比較を置いています(画像は `screenshots/redesign/logo-oneline.jpg`)。
  - **「S」の字**: 上の 3 段(ドーム・波・パティ)を、上から順に見ると、1d は、左でつなぎ、右でつなぐので S の字になります。1a は、右でつなぎ、左でつなぐので、鏡文字の S に見えます。
  - **線の始まりと終わり**: 1a は、どちらも左側(文字と反対)、1d は、どちらも右側(文字の方)にあります。ただし、利用者から、「線の始まりは、もっと柔軟でよい。真ん中から始めてもよい」と聞いたので、始まりの位置そのものは、決め手にしていません(「一筆書きの黄色い線 1 本・外枠なし」のレパートリーの探索は、始まりを自由にして考えています)。
  - 読みやすさ・実装の軽さは、1a と同じです(左右反転なので)。
  - 私の推奨は **1d を(i)黒い札の上に置く形**に変えました(S の字に見え、線の終わりが文字を向くため。差は向きだけで、好みの範囲でもあります)。

### ロゴ: 「黄色の線 1 本・外枠なし」の一筆書きのレパートリー(13 案)

利用者の要望「一筆書きの**レパートリーをもっと考えて欲しい。黄色の線一本。特に外枠もいらない**」への探索です。記号は、**黄色の 1 本の連続した線だけ**で、黒い札・縁取り・外枠・面の塗り・影は使いません(前の (i) 黒い札・(ii) 縁取りは、この探索の対象外です)。文字「BurgerStack」は、これまでどおりです。**線の始まりは自由**(利用者から、「真ん中から始めてもよい」と聞いたため、始まりの位置は決めていません)。

- 元: `redesign/logo-repertoire.html`(13 案の定義と、上位 3 案の詳細。評価は `redesign/logo-repertoire-eval.js`)。画像は、全案の一覧(`screenshots/redesign/logo-repertoire.jpg`。約 350KB)と、上位 3 案の詳細(`logo-repertoire-top3.jpg`。約 360KB)の 2 枚です。
- **13 案**: 01 コイル / 02 積み重ねた輪 / 03 S(バンズの形) / 04 B と S のモノグラム / 05 ごまの輪(ドーム) / 06 段ごとに違う波 / 07 かじった跡 / 08 笑うバーガー / 09 積み重なる虹 / 10 折れ線の S / 11 文字をはさむ線 / 12 B のバーガー / 13 登る階段。
- 各案を、白地に、大きく・ヘッダー(高さ 32px)・実寸 16px とその 8 倍・白黒・ファビコン(**札なし、透明な背景**。明るいタブと暗いタブの両方)で並べ、16px での読みやすさ・Burger と Stack の意味・実装の軽さ・評価のバーガーとの紛らわしさを、◎○△× で書いています。
- **上位 3 案**(私の推奨の順): **03 S(バンズの形)**(16px でいちばん読め、頭文字 Stack そのもの、楕円の弧 2 つで軽い)/ **07 かじった跡**(バーガーの手がかりが最も強い。16px ではかじった跡が見えず、評価のバーガーと形が似る)/ **02 積み重ねた輪**(Stack の意味が最も強く、最も軽い。バーガーの手がかりは弱い)。
- 惜しかった案: 06(Burger も Stack も伝わるが、16px で波がにじむ)。04 は、"BS" が、英語の俗語(bullshit の略)として使われる並びなので、避けたいです。
- **黄色の見えにくさ(外枠なしの範囲で)**: (A) いまのマスタード黄 #e9b824(白に対して 1.85:1。淡い)と、(B) ロゴだけに使う、わずかに深いマスタード黄 **#b88f13**(色相はそのまま、明るさだけを下げ、白に対して **3.01:1**)を比べました。白地では (B) の方が、はっきり読めます(特に明るいタブの 16px)。暗い面の上(暗いタブ・暗いヘッダー)では、(A) の方が見やすく(8.70:1 対 5.35:1)、**黄色の線だけで、外枠なしで成立します**。私の推奨は、白地に置くなら (B)、暗い面に置くなら (A) です。ただし、**(B) は、色を 1 つ増やす提案なので、利用者の判断が要ります**。(B) を採らないなら、(A) のままで、白地では淡くなることを受け入れることになります。
- **線の太さ**: 上位 3 案で、太さ 4〜12 を比べました。最適は、03 が 8〜9、07 と 02 が 7〜8 です(太いほど 16px で読める一方、折り返しやすき間がつぶれます)。
- **線が描かれていく動き**(実際に動く見本): `redesign/logo-motion.html`(約 6KB。ブラウザで開くと、上位 3 案が描かれます)。SVG の `stroke-dashoffset`(path に `pathLength="1"` を付けて、長さを測らずに描く)で、`prefers-reduced-motion` のとき(OS の「視差効果を減らす」など)は、動かさず、最初から描き終えた形で出します。Playwright で、通常(1.2 秒で描き終わる)と、動きを減らす設定(最初から描き終えた形)の両方で、動作を確かめました。「もう一度、描く」ボタンと、(A)・(B) の色の切り替えつきです。注意: 丸い端が描き始めに点として見えないよう、`stroke-dasharray: 1 2` と、`stroke-dashoffset: 1.02`(少しだけ余分)にしています。ページを移るたびに動かすとうるさいので、サインイン画面などで、1 回だけにするのが良いです。実装で使うかは、利用者が決めます。
- `.penpot` は、この PR でも更新していません。

### ロゴ: コイルをバーガーに近づける案(A)と、かじった跡の比較(B)

利用者の要望「コイルをもう少しハンバーガーに近づけたものを作れないか。それと、齧った跡を比較したい」への探索です(**最終化の作業は、この比較が終わるまで保留**しています。全画面のヘッダーの差し替え・スクリーンショットの撮り直し・`.penpot` の更新は、まだ行っていません)。制約は、これまでと同じです(黄色の線 1 本・外枠・面・縁取りなし。色は白地が深い黄 `#b88f13`、暗い面が今の黄 `#e9b824`)。

- 元: `redesign/logo-bite-coil.html`。画像は、A(コイル 6 案 + 基準の 01)が `screenshots/redesign/logo-bite-coil-a.jpg`、B(かじった跡 8 案 + かじったコイル 2 案)が `logo-bite-coil-b.jpg` です(各 約 260〜340KB)。
- **A. コイル**: 外形をバーガーの形(一番上をドームにして閉じる、輪の幅を変える)にし、間の 1 周をレタスのフリル・チーズのたれ(三角)にする、線の終わりをごまのカールにする、を試しました。**推奨は A6(パティがはみ出す)と A1**(16px で、ドームと帯 2 本として読め、外形がバーガーに近い。path は約 670 文字・3 次曲線 22 本で軽い)。A3(フリル)は 32px 以上でいちばんバーガーらしい一方、16px でにじみ、path も約 2.5KB。A4(チーズのたれ)は牙に見える。A5(ごまのカール)は、小さいと消える。**07 系(輪郭の一筆書き)より、16px の読みやすさは低く、帯どうしのすき間が狭くなります**。
- **B. かじった跡**: B1(いまの 07)/ B2 左上 / B3 手前の下側 / B4 2 回 / B5 大きなかじり / B6 ごく控えめ / B7 歯形つき / B8 断面が見える / B9・B10 かじったコイル。**16px で欠けとして読めるのは、B1・B2・B4・B5・B7(ぎざぎざとして)・B8**。B3(角の丸みに紛れる)・B6(読めない)・B9・B10(帯に混ざる)は、16px で欠けが読めません。
- **Apple のロゴとの連想**(法的な判断はしません): かじった形は、Apple のロゴ(右側がかじられたりんご)と連想が重なりえます。形はバーガーで、直接似てはいませんが、「かじった跡がロゴの特徴」という点で、同じ連想が生まれます。右上の半円(B1・B4・B5)は、位置も形も近く、連想が最も強い。連想を下げるなら、欠けの位置を変える(B2 左上)、かじった縁の描き方を変える(B8 断面・B7 歯形)。
- **かじった跡の推奨**: **B8(断面が見える)と B2(左上のかじり)**。B8 は、16px でもはっきり読め、「食べかけ」の物語が強く、Apple の連想も弱まる(弱点: 上が大きく欠けて不完全に見える)。B2 は、B1 と同じ読みやすさで、位置が違う(弱点: 視線の入口が欠けて少し落ち着かない)。
